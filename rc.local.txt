1 - Add this to rc.local:

if  [  ! -d /var/lock/bash ]; then
        mkdir   --verbose /var/lock/bash        ;       ## 2016-07-25
fi

2 - do this command:

systemctl start rc-local ;
 systemctl reload-or-restart  rc-local ;


